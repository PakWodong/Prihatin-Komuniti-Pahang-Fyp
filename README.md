# Prihatin-Komuniti-Pahang-Fyp
Run python -m venv myenv

then myenv/scripts/activate

then pip install -r /path/to/requirements.txt

then py manage.py runserver

then go to http://localhost:8000/
